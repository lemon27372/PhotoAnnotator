# 开发日志 - 2026-09-09

> 本日为 09-08 P3 会话的延续（凌晨进行）；因 commit 时间戳跨日，按「开发日建档」约定单独建档。

## 今日事项

### 交互调整 A：移除浏览工具，中/右键全局平移（用户反馈）

> 体验反馈：放大图片做框选/文字标注后，要移动图片需切回「浏览」按钮再拖动，繁琐。

- [x] **彻底移除浏览工具**（产品决策，用户确认）：
  - 工具栏删「浏览」按钮、快捷键 `0` 删除、`Tool::Browse` 枚举删除
  - 工具 id 重排：0 矩形 / 1 椭圆 / 2 箭头 / 3 画笔 / 4 文字
  - 数字快捷键保留旧键位习惯：`1-5` = 矩形…文字（键值 -1 映射）；字母 r/o/a/p/t 不变
- [x] **平移 = 任意工具下按住中键或右键拖动**：
  - Slint TouchArea pointer-event 扩展 middle/right down+up（`PointerEventButton` 支持 Left/Right/Middle，查证 i-slint-common enums.rs）
  - 新属性 `pointer-button`（0/1/2 = 左/中/右）传 Rust：非 0 进入 `Interaction::Panning`（先 ensure_free 固化 fit，无跳变）
  - 中/右键拖动前若文字输入框打开 → 丢弃草稿（不误提交，锚点不随平移）
  - 左键保持当前工具语义（绘制/文字落点）；滚轮缩放不变
- [x] 可发现性：工具条右侧常驻提示「中键/右键拖动 = 平移 · 滚轮 = 缩放」；切工具状态栏消息附平移提示
- [x] 文档同步：main.rs 头部交互模型、`doc/需求.md` v1.3

### 交互调整 B：默认「指针态」— 不默认进入工具（用户反馈）

> 反馈：A 把默认工具定为矩形后，启动/切图即落在绘制工具上不合适（怕误画、想先看图）。

- [x] **指针态（active-tool = -1）= 新默认**：启动与每次切图（apply_load_result）后自动回指针态
  - 指针态左键拖动 = 平移（等同旧浏览能力，但没有浏览按钮）；不绘制、不弹文字输入框
- [x] 工具按钮改 **toggle + checked 高亮**：点进入该工具；再点同按钮退出回指针态
- [x] Esc 层级插入「退出当前工具」：关最近面板 > 取消文字输入 > 取消绘制 > **退出工具回指针态** > 退出极简 > 提示
- [x] 键盘 1-5 / r o a p t 激活工具不变（无 toggle）；标注工具下平移仍 = 中/右键
- [x] 文档同步：`doc/需求.md` v1.4

```
启动/切图 → 指针态(左键平移) --点工具按钮/快捷键--> 工具(左键绘制) --同按钮/Esc--> 指针态
                   ↑__________________________________________________|
```

### 网格视图修复：内容下沉根因 + 悬停完整文件名（用户反馈 + 截图定位）

> 反馈：网格模式缩略图样式正常，但内容「突然往很下面移动」，且名称显示不全（要悬停提示完整名）。

- [x] 初版尝试：网格从 ScrollView+GridLayout 改为 ListView 行模型（每行 3 格等分）——**无效**
- [x] **根因 1（下沉）：`visible:false` 的 ListView 仍按隐式内容高度占布局空间**
  - 列表/网格两个 ListView 靠 `visible` 互斥切换，隐藏方（约 10 行 × 46px ≈ 520px）把可见视图推到侧边栏底部
  - 截图像素密度曲线佐证：y≈630 才有内容、上方整段空白
  - 修复：改**元素级 `if view-mode == ...`** 条件实例化——false 分支完全不创建、不占布局
- [x] **根因 2（Tooltip 不触发）：同区域多个事件接收者时 hover 只派发给最顶层**
  - 现象二分（工具栏隔离 Tooltip 正常 / 条目内不触发）→ Tooltip 声明在条目 TouchArea 之前（z 更低），hover 被 TouchArea 独占
  - 查证 i-slint-core `items/input_items.rs`：has_hover 由 MouseEvent 分发设置（hit-test 栈顶）
  - 修复：**Tooltip 移到条目内 TouchArea 之后声明**（TooltipArea 顶层收 hover；其不消费事件，点击穿透仍达 TouchArea，单击切图不受影响）
- [x] 交付：列表行 & 网格格均带完整文件名 Tooltip（白底圆角小窗）；开发者实测通过

### 调试方法论（无视觉模型环境，记录备用）

- 窗口标题临时版本指纹（`dev-r7`）确认用户运行的二进制
- 隔离 Tooltip（非 ListView 区域）二分 hover 归属问题
- 截图像素分析代替看图：按行内容密度曲线 + 横向色块分布（System.Drawing 采样），OCR 对 UI 无效

## 当日提交

```
00651b8 feat: 移除浏览工具-中键/右键任意工具下拖动平移(左键保留工具操作)
da571ee docs: 移除浏览工具-需求同步v1.3+日志追加
6e0931b feat: 默认指针态(active-tool=-1)-切图自动退出工具+工具按钮toggle高亮+Esc退出
4178d74 docs: 默认指针态-需求同步v1.4+日志追加
ec2f9a8 fix: 网格视图改为ListView行模型从顶部起排+悬停Tooltip显示完整文件名
8fff7f6 fix: 网格下沉根因(visible隐藏ListView仍占布局→改if条件化)+Tooltip悬停(hover被TouchArea独占→Tooltip后置声明)
```

## 待办

- 开发者手动 `git push`（本地领先 origin 14 提交：09-08 ×8 + 09-09 ×6）
- P4：过滤排序、完整状态管理 UI（含空目录清画布、虚拟化网格）；马赛克/序号仍 ⬜
- 存量：`font_available`/`image_to_canvas` 死代码 warning（非本次引入，未处理）

## 备注

- 需求.md 已同步 v1.3（移除浏览工具）/ v1.4（指针态默认）
- 网格 if 条件化后：列表/网格切换 = 销毁重建 ListView（滚动位置不保留，可接受）；Tooltip 顺序约定 = 放 TouchArea 之后

---

## 追加（同日续）：极简模式补全 — ←/→ 切图、窗口贴合图片、画布布局修复

> 反馈链路：极简模式"工作区空白" → 补 ←/→ 键盘切图；随后要求窗口贴合画布；发现"侧边栏空出但画布不扩展"。

### 今日事项

- [x] **←/→ 键盘切图**（极简模式下浏览工作目录，任意模式可用）：
  - Slint `@keys(LeftArrow/RightArrow)`（键名查证 i-slint-common key_codes.rs：Slint 名字 LeftArrow/RightArrow）→ `prev-image/next-image` 回调
  - `step_image`：按 `current_index ± 1` 异步加载（持久化旧标注 + 预取沿用）；无当前图时 → 从 0、← 从末张；边界提示"已是第一张/最后一张"；文字输入时禁用（不劫持光标）
  - 极简无状态栏 → 切图后 `flash_tip` 底部提示 `文件名 · 序号/总数`（3 秒熄灭；tip timer 抽为共享，进入极简/切图共用）
- [x] **极简模式窗口贴合图片**（用户需求）：
  - 进入 Ctrl+M：记录 `window().size()` → `minimal_fit_window`（目标 client ≈ 图片 + 18px 画布留白，等比缩放、cap 屏幕 95%，`set_size(LogicalSize)`）
  - 退出：`minimal_restore_window` 恢复原尺寸
  - 屏幕上限取主屏 `GetSystemMetrics`（`unsafe extern`，edition2024 语法）
  - 切图不跟随窗口（避免跳动）
- [x] **极简进入自动「适应窗口」居中**（用户反馈）：
  - 清除进入前缩放/平移残留，`enter_fit` 图片完整居中
- [x] **画布布局修复（根因 3）**：`visible:false` 元素仍占布局空间
  - 现象：极简模式侧边栏区域空出（残影），画布不扩展
  - 与网格下沉同根源：visible 仅隐藏绘制、不释放布局占位
  - 修复：工具条/侧边栏/状态栏三处 `visible: !minimal-mode` → **元素级 `if !minimal-mode`**（彻底移除），画布 `stretch` 占满

### 当日提交（追加）

```
fa53e55 feat: 极简模式增强-←/→切图+窗口贴合图片+进入自动居中+侧栏/工具条/状态栏改if条件化释放布局
```

### 待办更新

- 本批 ✅（←/→、窗口贴合、居中、布局修复，开发者验证通过）
- 开发者 push（本地领先 origin 已增至 16 提交）
- P4 不变；存量 dead-code warning 未处理

---

## 追加（同日续 2）：文件管理 — 重命名 / 覆盖原图保存 / 打开目录选择 / 目录打开防卡顿

> 需求：工作区（图片文件）重命名；保存直接写回原图（不再另存 `_annotated.png`）；GUI 选文件夹作为工作区；高清图多时打开目录卡顿。

### 今日事项

- [x] **图片文件重命名**（右键列表/网格图片行）：
  - 浮层输入新名（InlineTextInput + FocusScope：打开即聚焦、Enter 确认、Esc/取消关闭；focus 归位回画布快捷键层）
  - 校验：非法字符（`\ / : * ? " < > |`）/空名/`.`/`..`/同名拒绝；**扩展名强制与原一致**（未输入自动补）
  - 执行：磁盘 `fs::rename` → DB `files.path` 同步（file_id 不变，标注/缩略图关联保持）→ 列表/当前图路径刷新
  - Slint 踩坑：`forward-focus` **只接受直接元素引用，不支持三元表达式**（focus_handling debug_assert 崩溃）→ 改为静态 forward + 事件里 `.focus()`
- [x] **保存 = 覆盖原图**（产品决策确认：放弃旧「不修改原始图片」约束）：
  - 按原文件格式编码写回（PNG 保留 RGBA 无损；JPG/WebP/BMP/GIF 转 RGB，有损重压缩）
  - 原子写：临时文件 + 替换（防写坏）；保存=提交点：标注层清空（烧入原图）、撤销历史重置、DB 标注/缩略图缓存失效重建
  - 防双重叠加：合成基底（`image_rgba`）更新为含标注结果，DB annotations 清除，再打开不会 overlay 重复
  - **保存后台化防卡顿**：合成/编码/写文件移到后台线程（快照 store items + 原图 RGBA）；发起时先持久化 DB（期间切图不丢）；主线程收尾剥离已烧录段（保留保存期间新增 Δ）、置 done、刷新列表
- [x] **「打开」按钮 → 系统目录选择**（GUI 打开工作区）：
  - rfd 需联网下载（不可用）→ **Windows 原生 `SHBrowseForFolderW`**（shell32/ole32 FFI，零依赖离线可用）；COM STA 初始化；返回路径 → open_workspace + 加载首图
- [x] **目录打开防卡顿（高清图多时）**：
  - 根因：rebuild_sidebar 主线程同步为每张图解码生成缩略图 + 每文件独立开 DB 连接
  - 缩略图生成**后台线程串行**（先占位/DB 缓存先行；未命中收集 jobs，逐张生成 → 80ms 轮询增量 set_row_data 回填 file-list/grid model；round 序号丢弃过期轮次）
  - DB 批量：`upsert_files_batch` / `file_statuses_batch`（单连接，替代 N 次 open）；删除闲置 `file_status`/`composite_png`

### 当日提交（追加）

```
a4f70da feat: 图片重命名(右键行)+覆盖原图保存(原格式编码)+保存后台化防卡顿
a3c9981 feat: 打开文件夹对话框(原生SHBrowseForFolder) + 缩略图后台生成渐进回填 + DB批量存取(目录打开不卡顿)
```

### 待办更新

- 本批 ✅（重命名/覆盖保存/后台保存/打开目录/缩略图后台化，开发者验证通过）
- 打包 release exe 供真机测试
- 开发者 push（本地领先 origin 已增至 20 提交）
