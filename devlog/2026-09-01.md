# 开发日志 - 2026-09-01

## 今日事项

### 项目启动与文档
- [x] 需求文档 v1.0 定稿（基于《图片标注工具需求文档V2》整理）
- [x] 开发规范、技术规范、设计规范、执行步骤文档创建
- [x] 开发日志目录创建
- [x] 场景确认：测试人员标注手机测试错误截图，以"项目/文件夹"为单位管理
- [x] IDE 选定 VSCode（rust-analyzer / CodeLLDB / Slint 插件）
- [x] 新增功能需求：极简模式（Ctrl+M 全键盘沉浸式标注）+ 需求/设计/技术规范同步
- [x] 界面原型渲染：完整模式 + 极简模式两版（Edge headless）
- [x] Git 仓库初始化；协作约定：**AI 只 commit，开发者手动 push**
- [x] VSCode 推荐扩展清单（.vscode/extensions.json）

### 第一阶段：环境搭建 ✅ 完成
- [x] rustup 1.29.1 + cargo/rustc 1.98.0 (stable-x86_64-pc-windows-msvc)
- [x] MSVC Build Tools（VS2022 Community 含 VC++ 组件）
- [x] cargo init 骨架（photo_annotator, edition 2024）
- [x] release 优化（opt-level=3 / lto=fat / codegen-units=1 / panic=abort / strip）
- [x] .cargo/config.toml 清华镜像（crates.io 直连 SSL 失败，沙箱 TLS 受限 → 开发者环境编译）
- [x] 目录结构 src/{main,storage,canvas} + ui/app.slint + build.rs + .vscode/settings.json
- [x] 三件套最小验证通过（Slint 窗口 + TinySkia 自检图 + SQLite 4 张表）
- [x] 编译修复记录：slint-build 依赖缺失 / Slint padding 语法 / 布局无需 import / tiny-skia 0.11 API（Paint.shader、fill_path、stroke_path）

### 第二阶段：核心标注管线 进行中（12 commits，本地领先远程 8 个）
- [x] 图片加载（image crate 裁剪特性 png/jpeg/bmp/gif/webp → RGBA8 → Slint Image；缓存 raw RGBA 供导出）
- [x] 画布显示（Image + image-fit: contain + vertical-stretch）
- [x] 视图交互状态机：自适应(fit 常态) ↔ 自由(free)
  - 拖动/滚轮缩放(鼠标锚点)进入自由；「重置」回自适应；窗口 resize 自动回自适应（Timer 300ms）
  - 坐标换算 ViewTransform（image_to_canvas / canvas_to_image / zoom_at）
- [x] 标注图元（图片像素坐标存储，缩放平移不漂移）
  - 矩形：Rectangle 原生预览
  - 椭圆：BoxShape 共用结构 + Path(SVG commands) 预览
  - 箭头：线段+实心三角头；线宽 5px（ARROW_STROKE_WIDTH）；修圆头凸出盖箭尖(改 Butt)
  - 画笔：折线采样（按屏幕位移 0.5px/点）+ 圆头圆角
  - 预览性能：拖动只更新 Slint 元素属性，不重建标注层位图
- [x] 手势：仅左键触发；中/右键过滤（PointerEventButton.left）
- [x] 撤销（快照栈）：仅撤销；**产品决策：不做重做；保存=提交点，保存后清撤销历史**
- [x] 保存：原图+标注合成 PNG（straight→premul 转换）→ 同目录 `*_annotated.png`
- [x] 复制：合成 RGBA → 剪贴板（arboard）
- [x] 键盘快捷键层（FocusScope + KeyBinding @keys 语法）
  - 数字 0-5 / 字母 r,o,a,p,t 切工具；Ctrl+Z/C/S；Esc 层级
  - 焦点管理：点击画布/按钮后 canvas-fs.focus()；文字输入时快捷键禁用
- [x] 极简模式：Ctrl+M 隐藏工具条/状态栏 + 提示条 3 秒消失（Timer SingleShot）
- [x] 文字工具：自建 InlineTextInput（FocusScope+TextInput+forward-focus，点击落点自动聚焦、中文 IME 可用）
  - 点击他处=提交旧文字并开新框；导出用 fontdue + simhei 栅格化
- [x] **第二阶段核心标注管线完成**（浏览/矩形/椭圆/箭头/画笔/文字 + 撤销/保存/复制 + 快捷键 + 极简模式）
- [ ] 后置：马赛克/序号（V1.5）、H 帮助浮层（极简模式）、文字字号/颜色调节

### 第三阶段：工作目录模块 进行中
- [x] 设计文档 `doc/第三阶段-工作目录设计.md`（形态决策：目录导航式替代真树；P1+P2 范围）
- [x] 标注模型 serde 序列化（annotations 表 JSON 存取：to_json/from_json/set_items）
- [x] DB 扩展 CRUD：workspaces(touch)/files(upsert/status)/annotations(save/load)/thumbnails(cache)
- [x] workspace 模块：目录扫描（扩展名过滤+排序）+ 缩略图生成（image thumbnail → PNG 缓存）
- [x] **布局改造**：主窗口两栏（工作目录侧边栏 236px + 画布），ListView 图片列表（缩略图 40x30 + 文件名 + 状态点：灰/蓝/浅灰）
  - 修复：行内容垂直居中（VerticalLayout alignment:center 包装）
  - Slint 踩坑：Text 用 `overflow: elide`（非 elide）；ListView for 循环 `entry[idx]`
- [x] 命令行支持目录参数：`photo_annotator <文件夹>` → 打开工作目录 + 自动加载首图
- [x] **标注持久化（P2）**：切图前存旧标注 → 加载新图还原（切走切回不丢）
- [x] 保存后状态置 done 并刷新列表（files.status）
- [x] **异步切图**（多轮优化，解决卡顿/慢）：
  1. invoke_from_event_loop 方案 → Send 约束失败（Slint Image/Rc 非 Send）
  2. 改为「后台线程解码 + 跨线程队列 + 主线程 Timer 轮询」模式
  3. 竞态防护：AtomicU32 序号（edition2024 `gen` 是保留字 → 命名 load_seq）
  4. **邻图预取缓存 ±2**（解码结果缓存 HashMap，命中即秒开）
  5. 空标注时跳过全图 overlay 重建（1x1 透明占位）
  6. 加载中浮层反馈（画布中央半透明提示）
- [x] 大图（2K/4K 壁纸）诊断结论：慢在解码+上传物理耗时，**与中文文件名无关**（全程 UTF-8 路径）
- [ ] P3：双击子目录导航/面包屑、最近工作区、列表/网格切换
- [ ] P4：过滤排序、完整状态管理 UI

## 待办事项

- 开发者手动 `git push`（第三阶段多个提交待同步）
- 第三阶段 P3/P4；后置：马赛克/序号、H 帮助浮层

## 备注

核心场景：测试人员标注手机测试的错误截图，以"项目/文件夹"为单位管理。IDE=VSCode，工具链就绪。**第二阶段（核心标注管线）收官**；**第三阶段（工作目录）P1+P2 完成**：侧边栏列表 + 标注持久化 + 异步切图（预取 ±2 + 加载反馈）。开发协作升级：依赖缓存齐全后，AI 可在沙箱内 `cargo check --offline` 自行验证编译，报错无需开发者贴出。
