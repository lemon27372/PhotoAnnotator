# 开发日志 - 2026-09-08

## 今日事项

### 第三阶段：工作目录模块 P1+P2
- [x] 设计文档 `doc/第三阶段-工作目录设计.md`（形态决策：目录导航式替代真树；范围 P1+P2）
- [x] 标注模型 serde 序列化（annotations 表 JSON：to_json/from_json/set_items）
- [x] DB 扩展 CRUD：workspaces(touch)/files(upsert/status)/annotations(save/load)/thumbnails(cache)
- [x] workspace 模块：目录扫描（扩展名过滤+排序）+ 缩略图（image thumbnail → PNG 缓存）
- [x] 布局改造：主窗口两栏（工作目录侧边栏 236px + 画布），ListView 图片列表（缩略图 40x30 + 文件名 + 状态点灰/蓝/浅灰）
  - 修复：行内容垂直居中（VerticalLayout alignment:center 包装）
  - Slint 踩坑：Text 用 `overflow: elide`（非 elide）；ListView for 循环 `entry[idx]` 索引语法
- [x] 命令行目录参数：`photo_annotator <文件夹>` → 打开工作目录 + 自动加载首图
- [x] 标注持久化（P2）：切图前存旧标注（persist_current）→ 加载新图还原（切走切回不丢）
- [x] 保存后状态置 done（files.status）并刷新列表
- [x] 异步切图（多轮迭代，解决切换卡顿/慢）：
  1. invoke_from_event_loop 方案 → Send 约束失败（Slint Image/Rc 非 Send）
  2. 改「后台线程解码 + 跨线程队列 + 主线程 Timer(80ms) 轮询」模式
  3. 竞态防护 AtomicU32 序号（edition2024 `gen` 保留字 → load_seq）
  4. 邻图预取缓存 ±2（HashMap<idx, Decoded>，命中即秒开；缓存保留 ±2 防内存膨胀）
  5. 空标注跳过全图 overlay 重建（1x1 透明占位）
  6. 加载中浮层反馈（画布中央半透明提示）
- [x] 大图（2K/4K 壁纸）切换慢诊断：解码+GPU 上传物理耗时，**与中文文件名无关**（全程 UTF-8 路径，系统层自动转 UTF-16）
- [x] 开发日志拆分重构（按开发日建档：09-01/09-02/09-03/09-06/09-08）

## 当日提交

```
d5e3cdb feat: 第三阶段P1P2-工作目录侧边栏+标注持久化+异步切图
a2f9df1 perf: 切图加速-邻图预取缓存+空标注跳过overlay重建
34248a4 perf: 切图体验-预取扩至±2+加载中浮层反馈
5464bbb docs: 开发日志补录（+ 按日拆分重构）
0324241 docs: 开发日志按开发日拆分（09-01/02/03/06/08）
```

## 记录完备性审计（收尾）

- git 22 个提交全部按日归入 devlog（09-02×6 / 09-03×7 / 09-06×4 / 09-08×5）
- `doc/需求.md` 追加**实现进度同步 v1.1**（计划态 → 对齐实现：5 工具/撤销/保存/复制/快捷键/极简/工作目录 P1P2；马赛克·序号未做；产品决策备注）
- 行尾 LF、gitattributes 生效、工作区干净
- 规范确认：需求.md 修订节惯例（对齐 VideoShrink）；devlog 按开发日建档

## 待办

- 开发者手动 `git push`（多日提交待同步）
- 第三阶段 P3：双击子目录导航/面包屑、最近工作区、列表/网格切换
- P4：过滤排序、完整状态管理 UI

## 备注

协作升级：依赖缓存齐全后，AI 可在沙箱内 `cargo check --offline` 自行验证编译，报错无需开发者贴出。
日志规范：**每个开发日单独建档**（不再堆叠单文件）。
