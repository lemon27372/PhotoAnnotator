# 开发日志 - 2026-09-08

## 今日事项

### 第三阶段：工作目录模块 P1+P2
- [x] 设计文档 `doc/第三阶段-工作目录设计.md`（形态决策：目录导航式替代真树；范围 P1+P2）
- [x] 标注模型 serde 序列化（annotations 表 JSON：to_json/from_json/set_items）
- [x] DB 扩展 CRUD：workspaces(touch)/files(upsert/status)/annotations(save/load)/thumbnails(cache)
- [x] workspace 模块：目录扫描（扩展名过滤+排序）+ 缩略图（image thumbnail → PNG 缓存）
- [x] 布局改造：主窗口两栏（工作目录侧边栏 236px + 画布），ListView 图片列表（缩略图 40x30 + 文件名 + 状态点灰/蓝/浅灰）
  - 修复：行内容垂直居中（VerticalLayout alignment:center 包装）
  - Slint 踩坑：Text 用 `overflow: elide`（非 elide）；ListView for 循环 `entry[idx]` 索引语法
- [x] 命令行目录参数：`photo_annotator <文件夹>` → 打开工作目录 + 自动加载首图
- [x] 标注持久化（P2）：切图前存旧标注（persist_current）→ 加载新图还原（切走切回不丢）
- [x] 保存后状态置 done（files.status）并刷新列表
- [x] 异步切图（多轮迭代，解决切换卡顿/慢）：
  1. invoke_from_event_loop 方案 → Send 约束失败（Slint Image/Rc 非 Send）
  2. 改「后台线程解码 + 跨线程队列 + 主线程 Timer(80ms) 轮询」模式
  3. 竞态防护 AtomicU32 序号（edition2024 `gen` 保留字 → load_seq）
  4. 邻图预取缓存 ±2（HashMap<idx, Decoded>，命中即秒开；缓存保留 ±2 防内存膨胀）
  5. 空标注跳过全图 overlay 重建（1x1 透明占位）
  6. 加载中浮层反馈（画布中央半透明提示）
- [x] 大图（2K/4K 壁纸）切换慢诊断：解码+GPU 上传物理耗时，**与中文文件名无关**（全程 UTF-8 路径，系统层自动转 UTF-16）
- [x] 开发日志拆分重构（按开发日建档：09-01/09-02/09-03/09-06/09-08）

## 当日提交

```
d5e3cdb feat: 第三阶段P1P2-工作目录侧边栏+标注持久化+异步切图
a2f9df1 perf: 切图加速-邻图预取缓存+空标注跳过overlay重建
34248a4 perf: 切图体验-预取扩至±2+加载中浮层反馈
5464bbb docs: 开发日志补录（+ 按日拆分重构）
0324241 docs: 开发日志按开发日拆分（09-01/02/03/06/08）
```

## 记录完备性审计（收尾）

- git 22 个提交全部按日归入 devlog（09-02×6 / 09-03×7 / 09-06×4 / 09-08×5）
- `doc/需求.md` 追加**实现进度同步 v1.1**（计划态 → 对齐实现：5 工具/撤销/保存/复制/快捷键/极简/工作目录 P1P2；马赛克·序号未做；产品决策备注）
- 行尾 LF、gitattributes 生效、工作区干净
- 规范确认：需求.md 修订节惯例（对齐 VideoShrink）；devlog 按开发日建档

## 待办

- 开发者手动 `git push`（多日提交待同步）
- 第三阶段 P3：双击子目录导航/面包屑、最近工作区、列表/网格切换
- P4：过滤排序、完整状态管理 UI

## 备注

协作升级：依赖缓存齐全后，AI 可在沙箱内 `cargo check --offline` 自行验证编译，报错无需开发者贴出。
日志规范：**每个开发日单独建档**（不再堆叠单文件）。

---

## 追加（同日续）：第三阶段 P3 — 子目录导航 + 最近工作区 + 列表/网格

> 上文为 P1P2 收尾记录；P3 同日续开发完成，追加记录于此。

### 今日事项（P3）

- [x] **双击子目录导航 + 面包屑**
  - 形态延续「目录导航式」（非真树）：`AppState` 增 `dirs`（当前浏览目录一级子目录）+ `nav_stack`（面包屑栈，根在前）
  - 列表混排：`FileEntry.kind`（0=图片 1=子目录），子目录行列在图片前（前缀 `▸` 蓝字标识，无缩略图/状态点）
  - 行号换算：图片行号 = `dirs.len() + 图片索引` → UI 高亮改用新属性 `current-row`（替代原 current-index 直比）
  - 双击识别：Rust 端记录 `last_click_row + 时间戳`（同行 500ms 内=双击）；子目录单击仅提示、双击进入；图片单击即切图
  - 面包屑：`crumb-list [string]`（各级显示名），点击跳层级（truncate nav_stack + 重扫 + 加载首图）；末级加粗不可点
  - 切目录统一走 `rescan_current`（dirs+files+file_ids+thumb_cache 重置 + rebuild_sidebar）
  - **跨目录缓存竞态修复**：整图解码缓存按「图片索引」key，切目录（进入/面包屑/最近切换）时清空，防同索引命中旧目录图片
- [x] **最近工作区**
  - `db::list_workspaces(limit)`：按 last_opened 倒序；返回列表时过滤已删除目录（is_dir 检查）
  - UI：「最近」小按钮 → `open-recent` 回调拉取并显示浮层面板（绝对定位盖列表区，z 序=后声明）；条目（名+路径）点击 → 等价命令行打开目录；Esc/再点按钮关闭
  - 空历史显示「（暂无最近工作区）」
- [x] **列表/网格切换**
  - `view-mode`（0 列表 / 1 网格），侧栏标题下行 SegBtn 切换
  - 网格：**GridLayout + 重复 Row**（Slint 能力查证：`for row[ri]: Row` 官方支持、Row 内固定 3 格 `GridCell`）；非虚拟化 + ScrollView 滚动（P4 再上虚拟化网格）
  - `GridRow { c0/c1/c2: FileEntry }` 行模型，不足 3 格补 `kind=-1` 占位（透明不可交互）；file-list 与 grid-rows 同步维护（同一数据源）
  - 点击统一回调 select-file + clicked-file（行号），列表/网格共用同一分派逻辑
- [x] Slint 能力实测结论（供后续参考）：
  - GridLayout 子元素可设 `row/col` 但**须编译期常量**（不可用 `idx/3`）→ 网格必须「重复 Row + 固定格」
  - **无 `else if` 链**语法 → 互斥多态用独立 `if`（kind 三态）
  - Image 无 background/border-radius 属性（需包 Rectangle）
  - component 内 callback handler 引用自身属性须 `self.` 前缀；struct 字段支持数组（语法层面）
  - `for x in model: Row` 中 Row 是特殊行元素（非 item），官方测试佐证
- [x] 存量注意（未处理，记录待办）：进入「无图片目录」时画布保留上一张图（image_path 未清，此时 Ctrl+S 会保存残留图）——与 P1 空目录行为一致，后续再做空态清画布

### 待办更新

- ~~开发者手动 `git push`~~ → 仍待开发者手动 push（本地已领先 origin 7 提交）
- P3 ✅；P4：过滤排序、完整状态管理 UI（含空态清画布、虚拟化网格）

### 当日提交（追加）

```
33bdf1a feat: 第三阶段P3-子目录双击导航+面包屑/最近工作区/列表网格切换
```

---

## 追加（同日续 2）：交互调整 — 移除浏览工具，中/右键全局平移

> 体验反馈：放大图片做框选/文字标注后，要移动图片需切回「浏览」按钮再拖动，繁琐。

### 今日事项（交互调整）

- [x] **彻底移除浏览工具**（产品决策，用户确认）：
  - 工具栏删「浏览」按钮、快捷键 `0` 删除、`Tool::Browse` 枚举删除
  - 工具 id 重排：0 矩形 / 1 椭圆 / 2 箭头 / 3 画笔 / 4 文字；启动默认 = 矩形
  - 数字快捷键保留旧键位习惯：`1-5` = 矩形…文字（键值 -1 映射）；字母 r/o/a/p/t 不变
- [x] **平移 = 任意工具下按住中键或右键拖动**：
  - Slint TouchArea pointer-event 扩展 middle/right down+up（`PointerEventButton` 支持 Left/Right/Middle，查证 i-slint-common enums.rs）
  - 新属性 `pointer-button`（0/1/2 = 左/中/右）传 Rust：非 0 进入 `Interaction::Panning`（先 ensure_free 固化 fit，无跳变）
  - 中/右键拖动前若文字输入框打开 → 丢弃草稿（不误提交，锚点不随平移）
  - 左键保持当前工具语义（绘制/文字落点）；滚轮缩放不变
- [x] 可发现性：工具条右侧常驻提示「中键/右键拖动 = 平移 · 滚轮 = 缩放」；切工具状态栏消息附平移提示
- [x] 注释/文档同步：main.rs 头部交互模型、需求.md 同步 v1.3

### 注意（开发者验证项）

- 右键/中键在 Slint winit 后端是否稳定送达 pointer-event（理论上支持；实际手感待 cargo run 确认）
- 文本框输入时（IME）中/右键拖动：输入框可见即丢弃草稿，无残留

### 待办更新

- P3 ✅；本交互调整 ✅；P4 不变（过滤排序、完整状态 UI、空态清画布、虚拟化网格）
- 开发者 push（本地领先 origin 已增至 9 提交）

---

## 追加（同日续 3）：默认「指针态」— 不默认进入工具

> 反馈：v1.3 把默认工具定为矩形后，启动/切图即落在绘制工具上不合适（怕误画、想先看图）。

### 今日事项（指针态调整，用户确认方案）

- [x] **指针态（active-tool = -1）= 新默认**：启动与每次切图（apply_load_result）后自动回指针态
  - 指针态左键拖动 = 平移（等同旧浏览能力，但没有浏览按钮）
  - 指针态不绘制、不弹文字输入框（防误操作）
- [x] 工具按钮改 **toggle + checked 高亮**：点进入该工具；再点同按钮退出回指针态
- [x] Esc 层级插入「退出当前工具」：关最近面板 > 取消文字输入 > 取消绘制 > **退出工具回指针态** > 退出极简 > 提示
- [x] 键盘 1-5 / r o a p t 激活工具不变（无 toggle）；标注工具下平移仍 = 中/右键
- [x] 需求.md 同步 v1.4；main.rs 交互注释更新

### 状态流

```
启动/切图 → 指针态(左键平移) --点工具按钮/快捷键--> 工具(左键绘制) --同按钮/Esc--> 指针态
                   ↑__________________________________________________|
```

### 注意（开发者验证项）

- Button `checked`（std-widgets）高亮样式观感
- 指针态左键平移与极简模式组合（极简下工具快捷键可用、Esc 先退工具再退极简）

---

## 追加（同日续 4）：网格视图修复 — 行从顶部起排 + 悬停完整文件名

> 反馈：网格模式缩略图样式正常，但内容会「突然往很下面移动」，且名称显示不全。

### 今日事项（网格视图调整）

- [x] **网格改 ListView 行模型**（替换 ScrollView + GridLayout 重复 Row）：
  - 疑因：std ScrollView 基于 Flickable，内容小于视口时定位不可控 → 内容整体下沉
  - 现方案：`ListView for row[ri] in grid-rows` 每行固定 72px，行内 HorizontalLayout 三等分 GridCell（`horizontal-stretch: 1`），从列表顶部起排、虚拟化滚动
  - GridCell 不再固定 58x68，宽度由行等分（约 70px+），名称 Text fill 行宽（截断更少）
- [x] **悬停 Tooltip 显示完整文件名**（白底小窗，匹配设计浅色系）：
  - Slint `Tooltip` 内嵌自定义内容（`text` 属性只收 styled-text，动态字符串需自绘 child）
  - 网格格（kind>=0）+ 列表行都加了完整名 Tooltip（长名 elide 截断的兜底）
- [ ] 移除 ScrollView import（不再使用）；GridLayout/Row 语法不再用于网格（保留能力记录）

### 注意（开发者验证项）

- 运行中的 `photo_annotator.exe` 会锁 target 阻止 `cargo build` 链接 —— 需先退出再 cargo run
- 网格行顶部对齐观感、tooltip 出现延迟/白底样式
- 名称行高/字号（10px）是否偏小
